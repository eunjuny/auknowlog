# Auknowlog 개선 로드맵

이 문서는 기능을 나열하는 대신, 현재 문제와 선택 근거, 완료 조건을 함께 관리한다. 상태는 `예정`, `진행 중`, `완료`로 기록한다.

| 순서 | 개선 항목 | 해결할 문제 | 적용 방향 | 완료 조건 | 상태 |
| --- | --- | --- | --- | --- | --- |
| 1 | 제출형 풀이 UX | 선택 즉시 정답과 해설이 노출되어 학습 효과와 정답률 지표가 왜곡됨 | 모든 답을 선택한 뒤 한 번에 제출하고, 제출 후에만 채점·해설·저장을 허용 | 제출 전 정답 비노출, 제출 후 선택 잠금과 전체 채점, 저장 버튼 활성화 확인 | 완료 |
| 2 | pgvector 통합 테스트 | H2 테스트만으로 PostgreSQL 벡터 타입과 HNSW 검색을 검증할 수 없음 | Testcontainers의 pgvector PostgreSQL에서 Flyway V1~V3와 유사도 검색을 실행 | CI에서 마이그레이션·중복 판정·장애 폴백 테스트 통과 | 진행 중 |
| 3 | LLM 토큰·검색 예산 | 앞쪽 문서 청크와 최근 문제를 고정 개수로 전달해 관련성과 비용을 함께 통제하기 어려움 | 주제 관련 Top-K 청크 검색, 입력 길이 예산, 출력 토큰 상한, 사용자별 생성 한도 적용 | 대표 평가셋에서 품질을 유지하며 평균 입력 토큰과 p95 지연 감소 | 예정 |
| 4 | 인증·Git 내보내기 보안 | 사용자 구분이 없고 서버 프로세스가 로컬 Git 명령으로 원격 저장소를 변경함 | Spring Security로 데이터 소유권을 분리하고 GitHub OAuth/App 기반 명시적 내보내기로 전환 | 다른 사용자 데이터 접근 차단, 대상 저장소·커밋 확인 후 push, 감사 이력 저장 | 예정 |
| 5 | 전체 컨테이너화·CI | DB만 컨테이너화되어 실행 환경 재현과 배포 검증이 불완전함 | 백엔드·프론트엔드 Dockerfile, 전체 Compose, GitHub Actions 파이프라인 구성 | 새 환경에서 한 명령 기동, 빌드·테스트·통합 테스트 자동화 | 예정 |
| 6 | 학습 이력·대시보드·간격 반복 | 풀이와 복습 일정은 저장하지만 사용자가 학습 진척과 오늘 할 일을 보기 어려움 | 풀이 기록 목록·상세를 먼저 제공하고, 다음으로 오늘의 복습, 주제별 정답률, 반복 오답, 간격 반복 알고리즘을 제공 | 복습 대상 조회부터 재풀이·다음 일정 계산까지 한 흐름으로 동작 | 진행 중 |

## 1. 제출형 풀이 UX

### 선택 이유

정답률과 오답 복습은 사용자가 답을 모르는 상태에서 선택했다는 전제가 있어야 의미가 있다. 기존 화면은 선택 직후 정답 색상과 해설을 보여주어, 이후 문항의 풀이 행동과 저장된 정답률을 신뢰하기 어려웠다.

### 적용 내용

- 제출 전에는 선택 상태만 표시하고 정답·해설·근거를 숨긴다.
- 모든 문항에 답해야 `답안 제출`을 활성화한다.
- 제출하면 선택을 잠그고 정답·오답 색상, 해설, 근거, 전체 점수를 표시한다.
- PostgreSQL 풀이 기록과 Git 내보내기는 제출 후에만 허용한다.
- 새 퀴즈를 생성하면 선택·제출·저장 상태를 초기화한다.

### 검증 기준

- 프론트엔드 프로덕션 빌드 성공
- 제출 전 정답·해설 DOM 비노출
- 일부 문항만 선택했을 때 제출 비활성
- 전체 선택 후 제출하면 점수와 문항별 해설 표시
- 제출 후 선택 변경 불가
- 제출 전 풀이 기록·Git 저장 비활성, 제출 후 활성

### 검증 결과 (2026-08-31)

- `vite build` 성공
- 백엔드 전체 Gradle 테스트 성공
- 2문항 데모 시나리오에서 첫 문항만 선택했을 때 해설 0개, 제출 비활성 확인
- 전체 선택 후 제출 버튼 활성 확인
- 제출 후 해설 2개와 `2/2` 점수 표시, 선택지 잠금 확인
- 제출 전 비활성이던 풀이 기록·Git 저장 버튼이 제출 후 활성화됨을 확인

## 2. pgvector 통합 테스트

### 선택 이유

H2의 PostgreSQL 호환 모드는 일반 테이블과 유니크 제약을 빠르게 확인할 수 있지만 PostgreSQL 확장인 `vector` 타입, `<=>` 연산자와 HNSW 인덱스를 구현하지 않는다. 따라서 V3를 H2에서 제외한 기존 테스트만으로는 실제 배포 DB에서 마이그레이션과 검색 SQL이 동작한다고 증명할 수 없었다.

### 적용 내용

- `test`와 Docker가 필요한 `integrationTest` 소스 셋을 분리했다.
- Testcontainers 2.0.5가 운영 Compose와 같은 `pgvector/pgvector:0.8.6-pg16-bookworm` 이미지를 임의 포트에 실행한다.
- Flyway V1~V3를 빈 DB에 적용한 뒤 확장 버전, `vector(512)` 컬럼, HNSW 코사인 인덱스를 DB 카탈로그에서 확인한다.
- 실제 `QuestionVectorRepository` SQL로 유사 벡터는 0.90 임계값에서 차단되고 다른 벡터는 통과하는지 확인한다.
- 임베딩 검사에서 예외가 발생해도 SHA-256 정확 중복 검사와 문제 저장이 계속되는지 서비스 단위 테스트로 확인한다.
- GitHub Actions에서 백엔드 변경 시 `./gradlew check`를 실행해 단위 테스트와 실제 DB 통합 테스트를 함께 검증한다.

### 검증 결과 (2026-09-03)

- 단위·H2 테스트 12개 통과
- 실제 PostgreSQL 16 + pgvector 통합 테스트 2개 통과
- 전체 14개 테스트, 실패·오류·건너뜀 0개
- 로컬 `./gradlew check --rerun-tasks --no-daemon` 성공(17초)
- 라이브 OpenAI API 호출 0회, 토큰 비용 0원
- GitHub Actions 워크플로 구성 완료. 실제 원격 CI 성공 확인은 커밋·push 후 남은 완료 조건

세부 구조, 실행 방법과 테스트의 한계는 [pgvector·Testcontainers 통합 테스트](PGVECTOR_INTEGRATION_TEST.md)에 정리한다.
